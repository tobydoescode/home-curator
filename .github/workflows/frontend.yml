name: frontend

on:
  push:
    branches: [main]
    paths:
      - "apps/frontend/**"
      - "apps/backend/**"
      - ".github/workflows/frontend.yml"
  pull_request:
    paths:
      - "apps/frontend/**"
      - "apps/backend/**"
      - ".github/workflows/frontend.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/frontend
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v8.3.2
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: apps/frontend/package-lock.json
      - run: uv sync --all-extras
        working-directory: apps/backend
      - run: npm ci
      # Catches what tsc cannot: hook dependency arrays, conditional hook
      # calls, and impure render. It found a conditionally-called hook and
      # a Date.now() during render on its first run.
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test:run
      - run: npm run build
