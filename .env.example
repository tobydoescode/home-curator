# Copy to .env and fill in. Loaded by the Taskfile at the repo root.
#
# Where is your Home Assistant instance?
HA_URL=http://homeassistant.local:8123

# A long-lived access token from HA:
#   HA → your profile (bottom-left) → Security tab → Long-Lived Access Tokens
HA_TOKEN=
